# Roadmap

Deferred work includes package signatures and re-exports, package-defined
capability/schema declarations, dependency migrations, WASM transforms, a
language server and IDE plugins, additional renderers, Helm and Kustomize
importers, and a separate controller product.
