name = "acme/observability"
version = "1.0.0"
description = "Reusable observability declarations"
language_version = "v1alpha1"
license = "Apache-2.0"
sources = ["src/**/*.mosaic", "tests/**/*.mosaic"]
exclude = ["dist/**", ".git/**"]

[exports]
modules = ["ServiceMonitorModule"]
types = ["MetricsConfig"]
policies = ["requiredMetricsLabels"]
